server:
  port: 8081
spring:
  redis:
    port: 6379
    host: localhost
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:3306://localhost/~/test
    username: sa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

    database: h2

jwt:
  secret: "secretKey"
  validate: "1000 * 60 * 60 * 24"